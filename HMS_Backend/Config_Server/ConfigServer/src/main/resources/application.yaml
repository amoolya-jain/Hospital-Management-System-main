server:
  port: 9093

spring:
  application:
    name: CONFIG-SERVER
    
  cloud:
    config:
      server:
        git: 
          uri: https://github.com/amoolya-jain/hms-config
          clone-on-start: true